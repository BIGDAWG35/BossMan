# Dominoes Landing Page (Premium)

The `/` route serves as the public landing page (no auth required).

Key conversion principles:
1. Above-the-fold calm, premium tone
2. Member-only positioning (privacy + exclusivity)
3. Single primary CTA: Sign in
4. Three quick-look feature tiles, not a feature dump

Implemented in `client/src/routes/+page.svelte`.
